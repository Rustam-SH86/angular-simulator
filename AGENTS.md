# Project Instructions

Work step by step.

Do not implement the whole task at once.

First inspect the relevant files and explain the next step.

Do not modify any files until I explicitly ask you to apply changes.

Do not make unrelated changes.

Explain each change briefly before applying it.

Do not revert, reset, discard, or overwrite my existing changes unless I explicitly ask you to.

Use the current working tree as the source of truth.

For Angular tasks:
- Prefer the existing project structure and patterns.
- Reuse existing services, interceptors, guards, interfaces, and shared utilities when appropriate.
- Do not create duplicate logic if a similar implementation already exists.
- Keep changes minimal and scoped to the requested feature.
- Explain Angular-specific concepts briefly when they appear in the code.